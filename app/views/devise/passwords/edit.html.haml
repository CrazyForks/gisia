%h2.text-2xl.font-bold.text-center.mb-6 Change your password
.max-w-md.mx-auto.bg-white.p-8.rounded-lg.shadow-md
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
    = render "devise/shared/error_messages", resource: resource
    = f.hidden_field :reset_password_token
    .mb-4
      = f.label :password, "New password", class: "block text-sm font-medium text-gray-700"
      - if @minimum_password_length
        %em.text-xs.text-gray-500
          (#{@minimum_password_length} characters minimum)
      = f.password_field :password, autofocus: true, autocomplete: "new-password",                                                         |
        class: "w-full px-4 py-2 mt-1 border border-gray-300 rounded-md text-gray-800 focus:outline-none focus:ring-2 focus:ring-blue-500" |
    .mb-6
      = f.label :password_confirmation, "Confirm new password", class: "block text-sm font-medium text-gray-700"
      = f.password_field :password_confirmation, autocomplete: "new-password",                                                             |
        class: "w-full px-4 py-2 mt-1 border border-gray-300 rounded-md text-gray-800 focus:outline-none focus:ring-2 focus:ring-blue-500" |
    .flex.justify-center
      = f.submit "Change my password",                                                                                      |
        class: "w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-md transition duration-300" |
    = render "devise/shared/links"
