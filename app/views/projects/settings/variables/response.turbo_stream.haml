= turbo_stream.replace 'variables_content' do
  = render 'projects/settings/variables/variables_content'
= turbo_stream.update 'flash_messages' do
  - flash.each do |type, message|
    %div{class: "px-4 py-3 rounded-lg mb-2 flex items-center justify-between #{type.to_sym == :alert ? 'bg-red-50 text-red-800 border border-red-200' : 'bg-green-50 text-green-800 border border-green-200'}", 'data-controller': 'flash-message'}
      = message
      %button{type: 'button', class: 'ml-4 font-bold cursor-pointer hover:opacity-70', 'data-action': 'click->flash-message#close'}
        ×
