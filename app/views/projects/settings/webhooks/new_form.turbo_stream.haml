= turbo_stream.replace 'webhooks-content' do
  = turbo_frame_tag 'webhooks-content' do
    = render 'form', webhook: @webhook, project: @project

= turbo_stream.replace 'flash' do
  %div#flash{ class: 'fixed top-0 left-64 right-0 z-21' }
