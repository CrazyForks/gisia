- content_for :breadcrumb do
  - nodes = [*project_breadcrumb_nodes(@project), { label: 'Epics', path: namespace_project_epics_path(@project.namespace.parent.full_path, @project.namespace.path) }, { label: 'New Epic', path: nil }]
  = render 'shared/breadcrumb', nodes: nodes

%div{ class: 'w-full bg-white p-6' }
  %div{ class: 'w-full max-w-6xl mx-auto flex flex-col px-4' }
    %div{ class: 'mb-6' }
      %h1{ class: 'text-2xl font-semibold text-slate-900 mb-2' } New Epic
      %p{ class: 'text-slate-600' } Create a new epic to organize large features and initiatives.

    = render 'form'