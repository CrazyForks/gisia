# Release doc of 0.6.1

## What's new

1. Fix force push error for protected branches via SSH
2. Fix multiple line break characters being inserted in the description editor

## Actions

1. Change the gisia version to `image: gisia/gisia:0.6.1` in your `docker-compose.yml`
